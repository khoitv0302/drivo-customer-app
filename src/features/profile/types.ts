export interface UserProfile {
  name: string;
  phone: string;
  avatarUrl?: string;
}

export interface GreenRaceStats {
  rank: string;
  co2Trees: string;
  contributionVnd: string;
  totalKm: string;
  yearlyKm: string;
  currentYear: number;
}
